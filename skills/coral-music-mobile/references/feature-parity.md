# 珊瑚音乐移动端功能对等矩阵

## 目录

- [基线与状态定义](#基线与状态定义)
- [主导航](#主导航)
- [播放器与歌词](#播放器与歌词)
- [列表、本地媒体库与收藏](#列表本地媒体库与收藏)
- [下载、WebDAV 与本地能力](#下载webdav-与本地能力)
- [设置](#设置)
- [桌面能力的移动端等价](#桌面能力的移动端等价)
- [发布验收](#发布验收)

## 基线与状态定义

基线为 2026-07-15 的 `coral-music-desktop` React 实现及其开发历史。实现前必须复查对应桌面源码，避免参考文档滞后。

- `P0`：首个可用版本阻断项。
- `P1`：公开商店首发必须完成。
- `P2`：不阻断首发，但不得从最终一比一范围删除。
- 状态使用 `TODO`、`DOING`、`BLOCKED`、`DONE`；只有自动检查和要求的真机验证通过后才能标记 `DONE`。

## 主导航

| 能力 | 优先级 | 移动端行为 | 验收要点 | 状态 |
| --- | --- | --- | --- | --- |
| 搜索 | P0 | 已完成酷我/酷狗/QQ/网易云/咪咕歌曲与歌单搜索、分页、单来源与综合搜索、结果立即播放、QQ HTTPS 热门词、推荐歌手头像和本机搜索历史；酷我兼容单引号对象文本，酷狗歌单采用桌面同款 HTTPS 搜索端点；QQ 相册/歌手封面遵循官方 CDN MID 片段顺序，咪咕相对封面路径补全主机，QQ/咪咕兼容直接和嵌套封面字段；酷狗兼容公开端点以 `text/plain` 返回的 JSON；网易云歌曲封面在曲目模型层规范为 HTTPS，并兼容 `pic` 与顶层封面字段，歌单标签可搜索并打开网易云歌单详情。搜索平台独立于当前 User API，取链能力仅在点播时判断，综合搜索始终覆盖五个平台；歌曲封面与其它在线列表共用带 CDN User-Agent、Referer 和 HTTPS 归一化的加载组件；歌曲下载显示加入/重复反馈及下载状态，收藏显示真实状态；酷狗质量 hash 会传入 User API 取链 | 已有关键词时切换类型、来源、页码自动刷新 | DOING |
| 歌单广场 | P0 | 已实现酷我歌单的 HTTPS 分类、搜索、最热/最新、滚动触底分页与详情，并按 digest 类型解析真实详情 PID；QQ 支持热门/最新、滚动触底分页、详情及 HTTPS 关键词搜索，详情走官方当前 Musicu 接口；咪咕支持推荐、标签、滚动触底分页、详情及签名关键词搜索，曲目封面兼容专辑图片数组；网易云支持推荐、搜索与详情，并在空首选字段时回退 `picUrl`；酷狗支持 HTTPS 广场分页与详情页内嵌曲目，兼容当前分号结尾脚本。酷我补图任务随详情切换隔离。三个在线页面的平台菜单均从页面服务和当前 User API 的声明能力交集生成，不再各自硬编码。详情响应统一兼容 JSON 与 JSONP 文本，避免平台 Content-Type 变化导致解析失败。新页追加并按来源与歌单 ID 去重，下拉刷新、来源、排序、标签和搜索切换均重置到第 1 页。歌单可本地收藏快照、播放全部和下载全部；系统返回回到“更多” | 首次进入自动加载；接近底部自动加载更多；详情独立页面；重复触底不并发加载 | DOING |
| 排行榜 | P0 | 已支持酷我分页榜单、QQ、咪咕、网易云和酷狗榜单、来源切换、歌曲详情及播放全部；网易云使用可用的 V6 详情并兼容文本 JSON，酷狗从 HTTPS 榜单页提取内嵌曲目，避免旧 HTTP 端点和证书域名问题，并复用落雪桌面端资源权限接口以一次批量请求补齐榜单曲目封面；QQ 榜单封面按官方 CDN MID 地址生成，QQ 歌单的 qpic CDN 请求携带来源页；首页来源菜单含酷狗并按当前 User API 的播放能力过滤；榜单歌曲、搜索和歌单详情共用带来源 CDN Referer 的封面加载链路，网易云 EAPI 搜索保留资源卡图片并统一转 HTTPS，避免切换来源后 CDN 拒绝裸图片请求；发现页每日推荐按本地日期稳定选择当前来源的真实榜单，音乐电台从当前推荐曲目过滤不感兴趣歌曲后建立随机队列并立即播放 | 应用默认入口；同日推荐稳定；电台后续切歌保持随机；榜单/详情状态跨导航保留 | DOING |
| 我的列表 | P0 | 已实现 SQLite v2 用户列表、创建、改名、删除、拖动排序、歌曲加入/移除、去重和列表内播放；从“我的”压栈进入并提供可见返回及系统返回；默认/收藏/本地列表、批量操作及真机重启验收待补 | 创建、改名、删除、排序、歌曲管理完整 | DOING |
| 我的收藏 | P1 | 已实现歌曲收藏：播放详情、搜索、排行榜和歌单详情可收藏/取消收藏，点击后立即切换实心/空心状态并显示结果反馈；在线歌单与专辑均以本地曲目快照保存，收藏页可离线读取、播放、移除和返回上级 | 所有收藏只保存本机数据，不创建服务器数据源 | DOING |
| 音乐分类 | P1 | 已接入播放历史及自建列表/收藏/完成下载曲目的艺术家、专辑分类；类型和年份待完整标签读取后补齐；系统返回回到“更多” | 历史最多保留 1000 条，可删除和清空 | DOING |
| 下载 | P1 | 已实现在线歌曲单并发队列、歌单下载全部、暂停/Range 继续、取消、重试、离线播放和本地移除；推荐、搜索、歌单、播放队列和播放详情共用下载按钮，点击首帧显示加载动画，下载中持续显示状态，完成后显示勾；下载管理提供可见返回及系统返回；完成项显示真实文件名、格式和音质，并可选择更高可用音质重新下载；后台与批量统计待补 | 统计与真实状态一致；歌单快照去重，单曲失败不阻断其余任务 | DOING |
| 网盘资源 | P1 | 已实现单个 WebDAV 连接验证、安全凭据、本地目录浏览、子目录/上级进入、当前目录搜索、音频入队、鉴权播放和下载；系统返回回到“更多”；多账号待补 | 用户自行配置凭据；Range seek；不走在线音质切换 | DOING |
| 设置 | P1 | 只展示有效设置，桌面专属项显示移动端说明 | 每项能持久化并影响实际行为 | TODO |

手机使用底部主导航和“更多”，宽屏使用 `NavigationRail`；迷你播放栏始终位于内容与导航之间。Samsung SM-N986U / Android 13 已验证播放栏底部与 Navbar 顶部间隔 24px，不发生遮挡。发现页顶部音乐平台使用带图标和当前勾选状态的主题化锚点菜单，铃铛显示消息面板；当前没有消息数据源时显示“暂无新消息”。我的列表、我的收藏、音乐分类、歌单广场、下载、网盘资源和设置从“我的”页进入，支持页面返回按钮和系统返回；直接进入时返回“我的”。页面层级使用系统返回手势，不复制桌面侧栏。2026-07-16 起视觉层以 Coral Music 设计稿为基线重构（UI-01 DOING），不得影响现有功能入口和真实状态。

## 播放器与歌词

| 能力 | 优先级 | 行为基线 | 状态 |
| --- | --- | --- | --- |
| 四类来源 | P0 | 在线走 User API；本地、下载、WebDAV 直接使用各自 `localUri`，不进入 User API 或在线音质链。文件导入/WebDAV 发现待补 | DOING |
| 本地音频播放闭环 | P0 | 已实现文件选择、目录递归扫描、基础元数据、同目录 LRC、缺词时按歌名/歌手独立搜索、队列、历史和 `AudioEngine` 播放；Android 目录扫描按需请求媒体读取权限，拒绝或遍历失败会明确反馈；本地文件信息可解析格式/码率/采样率；分享入口与三端授权恢复待补 | DOING |
| 播放地址解析 | P0 | Android 真机已通过公开 LX User API 的真实 `musicUrl` 解析与播放；已保存的 HTTPS 音源在应用启动时自动恢复，不依赖进入音源管理；新安装且没有用户来源时自动配置公开落雪音源，已有用户来源优先。启动恢复、上次在线歌曲恢复和在线取链共用同一就绪屏障，发现页切换目录平台不重置 User API 运行时。QQ/咪咕曲目向脚本传递桌面兼容的 MID、版权和质量字段；QQ/咪咕补齐脚本协议的 `flac/320k/128k` 键，咪咕另支持 `ZQ24`。受限脚本可访问公开 HTTP/HTTPS 取链服务，但在 DNS 解析后拒绝本机、私网、链路本地及组播地址；最终媒体地址兼容桌面端的 HTTP/HTTPS。已实现 15 分钟内存缓存、一次过期刷新、本地/下载直连和 WebDAV 运行期鉴权头；跨来源换源待补 | DOING |
| 音质 | P0 | 已支持曲目声明与 User API 脚本 `qualitys` 能力的合并选择、真实重新取链和按曲目声明质量降级；Hi-Res、全景声和母带按音源实际声明展示；跨来源换源待补 | DOING |
| 队列 | P0 | 已实现替换、去重追加、非当前项删除、拖动排序、点击切歌、当前项高亮、队列数量和位置；替换队列保留用户当前播放模式；应用重启恢复队列、当前项和播放模式，但不自动播放或保存播放 URL | DOING |
| 播放模式 | P0 | 已实现列表循环、单曲循环、随机及本轮随机历史；随机模式的手动上一曲/下一曲和系统媒体切歌同样选择随机候选，列表循环与单曲循环的手动切歌按相邻项；同一轮播放的重复完成快照只推进一次，避免短队列绕回原曲；Android 修复版三模式真机回归及 iOS/鸿蒙系统媒体回归待补 | DOING |
| 生命周期 | P0 | 已实现最小播放/暂停/stop、seek、加载、自动下一首和失效音源跳过；Android 真机已通过真实搜索点播、详情切歌、后台持续和系统下一首，其余平台待补 | DOING |
| 进度 | P0 | 已实现 seek、倍速、应用内音量、15 秒/暂停/seek 进度保存，以及从播放历史继续；应用启动恢复和保持音调（平台支持时）待补 | DOING |
| 后台媒体 | P0 | 已接入 `audio_service` 前台媒体服务、通知/锁屏元数据、播放/暂停/seek、上一首/下一首队列路由及 music audio session；Android 真机已通过真实播放、后台持续、系统播放/暂停及后台下一首，三端及锁屏/耳机人工验收待补 | DOING |
| 播放详情 | P0 | 已实现封面卡片、歌曲信息、进度、主控、模式、质量、倍速和共享队列；队列显示当前播放态、封面、来源和保留的下载/移除/拖拽操作；播放/歌词可左右滑动切换，播放页顶部下拉退出；歌词页使用顶部歌曲信息、中部歌词、底部播放主控的固定分区 | DOING |
| 歌词数据 | P0 | 已支持本地 LRC，以及所有在线和本地曲目统一先通过 LrcLib 按“歌名+歌手”（可用时加专辑、时长）独立检索并选择最接近候选；未命中才按曲目元数据调用公开 QQ/酷我/网易云/咪咕/酷狗歌词端点。酷狗使用 HTTPS、按标题/歌手/时长选取候选，并将 KRC 解码为逐字时间；网易云在响应提供 YRC 时转换为逐字时间；歌词链路不调用 User API、音源脚本或原生 MethodChannel，因此切换音乐平台不会影响歌词请求。成功歌词保留会话内缓存，持久缓存和编辑歌词待补 | DOING |
| 本地歌词 | P0 | 已支持 `file://` 同目录同名、歌手 - 歌名、歌名 - 歌手、歌名的 UTF-8 LRC；本地未命中时按歌名、歌手走独立歌词搜索，所有本地/下载/WebDAV 均不请求 User API，下载/WebDAV 不联网。content URI、模糊匹配、其它编码待补 | DOING |
| 歌词呈现 | P1 | 已实现固定歌曲头、独立滚动歌词视口、底部进度和播放主控，以及 LRC 当前行/逐字高亮、翻译、自动滚动、无时间戳正文兜底和手动重试；字号/对齐待补 | DOING |
| 在线歌曲收藏 | P1 | 播放详情、搜索、排行榜和歌单详情共享同一收藏状态，收藏仅保存本地 SQLite | DOING |
| 音频可视化 | P2 | 播放详情内可关闭的轻量可视化 | TODO |
| 音效 | P2 | 系统允许时提供 EQ；不可用的平台显示能力状态 | TODO |
| 定时停止 | P1 | 播放详情支持 15/30/45/60 分钟后停止或当前歌曲结束后停止；仅本次会话生效 | DOING |

播放器错误必须区分：无音源、User API 未就绪、网络错误、格式不支持、权限错误、WebDAV 鉴权失败和系统中断。可恢复错误提供单次重试，不能无限循环。

## 列表、本地媒体库与收藏

| 能力 | 优先级 | 验收要点 | 状态 |
| --- | --- | --- | --- |
| 列表 CRUD | P0 | 已实现自建列表的创建、重命名、删除与持久顺序；清空、默认/收藏列表和三端真机重启验收待补 | DOING |
| 歌曲操作 | P0 | 已实现单首加入、去重、单首/批量移除、跨列表复制/移动、批量置顶和列表内播放；定位待补 | DOING |
| 排序与搜索 | P1 | 已实现用户列表与歌曲顺序持久化、列表内关键词搜索及按在线/本地/下载/WebDAV 来源筛选；仅完整列表视图允许拖动歌曲排序 | DOING |
| 去重 | P1 | 检测、复核、批量删除重复项 | TODO |
| 导入导出 | P1 | 兼容桌面列表数据格式，非法项返回可读报告 | TODO |
| 本地文件与目录导入 | P0 | 已实现文件选择与用户授权目录递归扫描；没有播放列表时可直接选择音频文件，自动创建并进入“本地音乐”列表；Android 选择目录后按需请求音乐和音频权限，不再静默返回空列表；导入后可进入现有列表、队列和播放；分享入口与重启授权恢复待补 | DOING |
| 元数据 | P0 | MP3/AAC(ID3)、FLAC、WAV、M4A、Ogg/Opus 已读取标题、歌手、专辑、年份与类型；支持格式的嵌入封面落入私有缓存，缺失时使用同目录封面；时长与三端实机格式矩阵待补 | DOING |
| 格式 | P0/P1 | P0: mp3/flac/wav/m4a/aac/ogg/opus；FLAC 已覆盖导入、标签/封面和本地 URI 直通。导入额外识别 dsf/dff/ac3/aif/aiff/alac/m4r/wma；Android 的 DSF/DFF 由非 GPL FFmpegKit 单线程异步转为临时 PCM WAV 后交给既有播放器，普通 WAV 直通，仅 DTS 伪 WAV 才转码，非 Android 明确不支持该适配。转码期间主播放按钮显示不可点击的加载状态；DSF/DFF 显示容器头中的 DSD 原始码率，DTS WAV 显示 WAV 字节率中的编码码率。当前未达到桌面端秒播，真实 DSF/DFF 播放、seek、切歌、后台及全格式 Android/iOS/鸿蒙实机矩阵待验收 | DOING |
| CUE | P1 | 已支持 FILE/TRACK/INDEX 01 单文件分轨，按分轨起点 seek、边界自动切歌并避免整轨重复导入；可解析 FLAC/MP3 总时长会补齐末曲边界，真机格式矩阵待验收 | DOING |
| 失败报告 | P1 | 统计候选、导入、重复、跳过和失败原因 | TODO |
| 播放历史 | P1 | 已在首次进入真实播放态时写入；重复歌曲更新最近时间、次数与最后位置，最多保留 1000 条；已实现读取、清空和历史内播放，真机重启验收待补 | DOING |
| 收藏 | P1 | 已实现歌曲添加、移除、恢复和列表内播放；在线歌单与专辑快照待补 | DOING |
| 分类 | P1 | 已按专辑、艺术家、类型、年份归组并进入歌曲列表；类型/年份仅显示真实导入标签 | DOING |
| 不感兴趣 | P1 | 已支持曲目级本地切换，并在播放全部建队前过滤；规则管理、导入导出和关键词规则待补 | DOING |

## 下载、WebDAV 与本地能力

| 能力 | 优先级 | 移动端约束 | 状态 |
| --- | --- | --- | --- |
| 下载队列 | P0 | 已实现单并发的等待、运行、暂停、完成、失败、重试和取消；重启时未完成任务保留为暂停、丢失完成文件转可重试失败；后台与三端恢复验收待补 | DOING |
| 在线歌曲下载 | P0 | 使用 Track 快照与所选音质创建本地任务，启动时取链，`.part` 成功后原子写入应用目录；不保存临时 URL 或脚本 | DOING |
| 歌单下载全部 | P1 | 已固定歌单快照去重入队，单曲失败隔离，并显示本次入队/跳过统计 | DOING |
| 自动开始 | P1 | 创建任务后立即进入前台单并发调度；不要求再次点击 | DOING |
| 文件命名 | P1 | 默认使用“歌名 - 歌手”，冲突时避开成品与 `.part` 文件；下载列表显示最终文件名、格式后缀和任务音质，升级下载保留旧文件；旧模板与自定义模板待补 | DOING |
| 附加内容 | P2 | 歌词文件、封面和平台可实现的标签嵌入 | TODO |
| 文件访问 | P0 | 下载使用应用目录；完成文件可通过系统选择器复制导出，仍需三端真机验证 | DOING |
| WebDAV 账号 | P1 | 已实现多个连接的命名、验证、保存、切换、删除与最近账户恢复；Authorization 仅安全存储 | DOING |
| WebDAV 浏览 | P1 | 已实现根目录、子目录、根目录约束的返回上级、可点击面包屑、当前目录搜索、刷新和音频筛选 | DOING |
| WebDAV 播放 | P1 | 已传递安全存储中的 Authorization 给播放器，并支持加入本地列表；真实服务器 Range seek 与过期凭据验收待补 | DOING |
| WebDAV 下载 | P1 | 已复用下载状态机并保留播放地址原始扩展名；真实服务端与三端恢复验收待补 | DOING |
| User API | P0 | Android 已实现受控 HTTPS 脚本请求、超时和大小限制，并通过公开 LX 地址导入、启用、真实取链与播放；新安装没有用户来源时自动配置公开落雪 URL，用户添加的来源始终优先；HTTPS 来源 URL/名称会安全保存并在启动时重新校验加载，本地 `.js` 文件与开发粘贴保持会话级。商店门控待补 | DOING |
| 本地备份恢复 | P1 | 设置、列表、历史和规则导出为本地文件；恢复前验证版本与完整性 | TODO |

## 设置

- `P0`：主题模式、语言、播放音质（已支持 SQ 默认和持久化选择）、播放模式、系统媒体音量（由操作系统统一管理）、后台播放、本地导入、下载目录、网络代理、User API。
- `P1`：播放详情、歌词、列表、下载、搜索、WebDAV、本地备份、不感兴趣、缓存、定时停止。
- `P2`：可视化、音效和高级诊断。
- 更新由应用商店负责；不复制桌面自动更新器。
- 桌面歌词设置映射为播放详情歌词、锁屏信息和平台支持的通知/悬浮歌词。
- 快捷键设置映射为耳机/蓝牙媒体按键，不允许用户覆盖系统保留手势。
- WASAPI 独占输出映射为音频路由与高解析能力展示，不宣称移动端独占输出。

## 桌面能力的移动端等价

| 桌面能力 | iOS | Android | 鸿蒙 |
| --- | --- | --- | --- |
| 托盘控制 | 控制中心/锁屏 | 媒体通知/快捷设置 | 媒体通知/控制中心 |
| 全局快捷键 | 耳机/蓝牙媒体事件 | MediaSession 媒体事件 | AVSession 媒体事件 |
| 桌面歌词窗 | 应用内歌词与系统媒体信息 | 应用内歌词，可选合规悬浮窗 | 应用内歌词，可选系统支持的悬浮能力 |
| 窗口控制 | 系统导航 | 系统导航 | 系统导航 |
| 文件拖放 | 文件选择器/分享 | 文件选择器/分享 | 文件选择器/分享 |
| WASAPI 独占 | 系统音频路由 | 系统音频路由 | 系统音频路由 |

## 发布验收

- Android `1.0.0+1` 正式签名 universal APK 已生成并通过 `apksigner` 证书校验；当前设备已有 debug 签名安装，正式包覆盖安装验收待卸载旧包后执行。AAB、iOS、鸿蒙和商店发布仍待继续。
- 九个入口的 P0/P1 行全部为 `DONE`，或附有产品批准的平台等价说明。
- 三端分别连续播放 60 分钟，覆盖锁屏、前后台、耳机事件、网络切换和来电/音频焦点中断。
- 在线、本地、下载、WebDAV 各完成播放、seek、队列切歌和进程恢复。
- 1000 首列表滚动、搜索、选择与队列操作无明显掉帧或长时间阻塞。
- 普通数据库、日志与崩溃报告中搜索不到测试账号、密码、Token 或脚本密钥。
