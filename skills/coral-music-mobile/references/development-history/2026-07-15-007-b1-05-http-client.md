# B1-05 建立 HTTP 客户端

- 阶段：Batch 1
- 状态：DONE
- 负责人：Codex
- 开始时间：2026-07-15
- 完成时间：2026-07-15

## 目标与范围

建立 Dio 客户端、15 秒超时、取消、状态码检查和稳定错误映射。依赖 B1-03、B1-04；不实现代理、缓存和凭据请求。

## 桌面端基线与方案

对照桌面 `services/musicSdk/request.ts` 的超时、取消和网络错误语义，移动端不复制 Node `needle/tunnel/zlib` 实现。

## 实际修改与验证

- 新建 `lib/core/http_client.dart` 和 `app_failure.dart`。
- 配置连接、发送和接收 15 秒超时，限定 2xx 状态，并映射取消、超时、断网和坏响应。
- 诊断信息只保留 scheme、host、port 和 path。
- 首轮测试发现清除 query 后仍残留 `?#`，改为重新构造安全 URI；脱敏测试通过。

## 风险、阻塞与后续

未加入请求正文或响应日志；代理、缓存和认证延后。关联 B2-02。
