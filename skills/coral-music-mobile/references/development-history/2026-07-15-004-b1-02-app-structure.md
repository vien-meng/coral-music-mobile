# B1-02 拆分应用壳

- 阶段：Batch 1
- 状态：DONE
- 负责人：Codex
- 开始时间：2026-07-15
- 完成时间：2026-07-15

## 目标与范围

将 `lib/main.dart` 的应用、导航壳和页面占位拆入 `app/` 与 `features/`，保持九个入口和自适应导航。依赖 B1-01；不实现数据库、音频或平台代码。

## 桌面端基线与方案

对照桌面 `routeConfig.tsx` 的九入口；Flutter 继续采用底栏、更多页和宽屏 Rail。只提取当前真实使用的组件，不建立空 feature 层。

## 实际修改与验证

- `lib/main.dart` 只保留启动入口。
- 新建 `lib/app/app.dart`、主题、路由、应用壳和占位页面。
- 排行榜与播放器按真实纵切分别放入 feature 目录，没有创建其余空 feature。
- Widget Test 验证手机导航、更多入口和宽屏 Rail；全部通过。

## 风险、阻塞与后续

首次测试发现 800×600 被错误判为宽屏并导致 Rail 溢出；修正为宽高共同判定，并用宽屏测试验证。关联 B1-06、B2-04。
