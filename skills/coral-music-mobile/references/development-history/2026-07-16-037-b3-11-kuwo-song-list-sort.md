# B3-11 酷我歌单排序

- 阶段：Batch 3 / Phase 2
- 状态：DOING
- 负责人：Codex
- 开始时间：2026-07-16
- 完成时间：未完成

## 目标与范围

将桌面端“最热 / 最新”排序接入酷我热门歌单广场。依赖 B3-05；不改变标签详情、收藏或多来源范围。

## 桌面端基线与实施

对照 `sdk/kw/songList.js` 的 `sortList` 与 `order` 参数。复用现有歌单请求、页面状态和分页：排序变更回到第一页并重新请求。

## 验收与恢复入口

- 收口时验证排序参数、切换重载、标签与排序组合。
- 恢复入口：`kuwo_playlist_service.dart`、`song_list_controller.dart`、`song_list_page.dart`。

## 当日实施进度

- 已增加 `hot` / `new` 请求参数、排序状态和页面下拉选择；选择排序或标签都会从第一页重新加载。
