# P7-05 Android 1.0.0 APK

- 阶段：Phase 7 / 收口与发布
- 状态：DOING
- 负责人：Codex
- 开始时间：2026-07-20

## 目标与范围

将移动端业务版本更新为 `1.0.0`、Android 构建号设为 `1`，在已完成 Samsung SM-N986U 真机冒烟后生成首个 Release 模式 APK。

当前 Android `release` 构建仍复用 debug signing config。本次产物只用于内部安装验证，不作为应用商店或公开长期升级渠道的正式签名包。

## 验证要求

- `pubspec.yaml` 为 `1.0.0+1`，APK 显示版本名 `1.0.0`、版本号 `1`。
- Release APK 构建成功并记录文件大小、SHA-256 和签名状态。
- 不把 debug 签名产物标记为商店发布完成。

## 当前结果

- `pubspec.yaml` 已更新为 `1.0.0+1`。
- 用户明确允许重新执行 Release 构建后，`flutter build apk --release --build-name 1.0.0 --build-number 1` 仍被外部审批服务 HTTP 503 拦截；Flutter/Gradle 进程未启动，没有生成新的 Release APK。
- 未使用直接 Gradle 或其它间接命令绕过审批。任务保持 `DOING`，待审批服务恢复后执行同一构建命令。
