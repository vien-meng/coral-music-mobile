# P7-05 Android 1.0.0 APK

- 阶段：Phase 7 / 收口与发布
- 状态：DOING
- 负责人：Codex
- 开始时间：2026-07-20

## 目标与范围

将移动端业务版本更新为 `1.0.0`、Android 构建号设为 `1`，在已完成 Samsung SM-N986U 真机冒烟后生成首个 Release 模式 APK。

Android Release 已接入仓库外的正式 JKS signing config；密钥文件和 `android/key.properties` 均为本机 `600` 权限，不进入 Git。

## 验证要求

- `pubspec.yaml` 为 `1.0.0+1`，APK 显示版本名 `1.0.0`、版本号 `1`。
- Release APK 构建成功并记录文件大小、SHA-256 和签名状态。
- 不把 debug 签名产物标记为商店发布完成。

## 当前结果

- `pubspec.yaml` 已更新为 `1.0.0+1`。
- 已生成并验证正式签名密钥，别名为 `coral-music-release`，证书 SHA-256 为 `63:2C:B7:60:4E:71:E2:98:7A:14:7F:2D:0E:69:C2:62:E5:CB:96:15:BD:85:52:9A:4F:6C:31:87:30:90:6A:7C`。
- Codex 审批服务曾多次返回 HTTP 503；用户在本机终端执行同一构建命令后成功生成 Release APK。
- APK 元数据为包名 `com.coral.music.mobile`、版本名 `1.0.0`、版本号 `1`，文件大小约 54 MB，SHA-256 为 `e8ac5f2d2eb211882c585003e68a506d1613baacf0ac8b8161abd8931cab80ab`。
- `apksigner verify --verbose --print-certs` 通过，使用 APK Signature Scheme v2，单一 RSA 2048 签名者与上述证书指纹一致。正式签名 APK 尚未覆盖安装到当前已安装 debug 签名的真机，避免破坏现有应用数据；Android 设备可卸载旧 debug 包后安装验收。
- 任务保持 `DOING`，三端发布和商店验收仍待继续。
