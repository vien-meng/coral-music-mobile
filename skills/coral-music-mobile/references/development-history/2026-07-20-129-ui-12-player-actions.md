# UI-12 播放页操作收口

- 阶段：UI / 播放详情
- 状态：DOING
- 负责人：Codex
- 开始时间：2026-07-20

## 范围

修复播放页收藏对齐、下载无反馈、重复的应用音量入口和含义不清的定时入口；将音质选择改为紧凑的底部选择面板。复用现有收藏、下载、播放器和定时控制器，不新增平台桥或依赖。

## 实施与验证

- `player_detail_page.dart` 只保留页面编排，并按播放控制、操作弹层、歌词和队列拆成独立文件；文件行数分别为 160、440、184、250、135。
- 收藏与其余操作统一 48 px 图标槽和标签样式；播放页移除两处应用内音量入口，系统媒体音量继续由操作系统统一管理。
- 下载首次入队等待持久化任务恢复完成，避免恢复结果覆盖新任务；播放页显示成功或重复反馈，并可进入下载管理。
- “定时”改为“定时停止”；音质选择改为可滚动底部面板，展示档位说明和选中态。
- `test/app_shell_test.dart` 增加收藏/下载对齐、无重复音量入口和音质面板回归。
- `dart analyze` 对本次改动文件无问题，`git diff --check` 通过。Flutter SDK 缓存位于工作区外且权限授权服务失败，`flutter test test/app_shell_test.dart` 尚未实际执行，状态保持 `DOING`。
