# B6-12 歌单下载批次统计

- 阶段：Batch 6 / Phase 5
- 状态：DOING
- 负责人：Codex
- 开始时间：2026-07-20
- 完成时间：未完成

## 目标、范围与依赖

歌单“下载全部”需要反馈本次实际入队数量与被跳过的重复/不支持曲目数量；已入队任务的异步下载失败继续由既有下载列表显示，不把“已入队”伪报为“已下载”。依赖 B6-01、B6-02。

不建设后台批次实体、通知或服务器统计；复用单首入队的去重和来源校验。

## 实际修改、验证与后续

- 单首 `enqueue` 现在返回是否实际创建任务；`enqueueAll` 顺序复用它并返回 `{added, skipped}`，因此重复、已完成或不支持来源不再被前置筛选和实际入队逻辑分别判断。
- 歌单详情提示真实显示入队数；若有跳过项，明确说明是重复或不支持，而不承诺已完成下载。后续实际下载失败继续在下载管理页按任务显示。
- `dart format`、`flutter analyze`、`flutter test test/download_controller_test.dart` 和 `git diff --check` 通过。
- 状态：DONE。后台调度、系统通知和跨平台真机批量下载仍由 B6-01/B6-02 收口。
