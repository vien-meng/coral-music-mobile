# P1-01 Flutter 与 Android CI

- 阶段：Phase 1
- 状态：DOING
- 负责人：Codex
- 开始时间：2026-07-20
- 完成时间：-

## 目标、范围与依赖

在每次推送和拉取请求中运行 Dart 格式检查、静态分析、单元测试和 Android Debug APK 构建。

使用 GitHub Actions 的标准 Flutter/Java 环境；不在 Linux CI 伪造 iOS 签名或鸿蒙 HAP 构建，二者仍使用对应平台的真实工具链验收。

## 实施记录

- 2026-07-20：开始创建最小 CI 工作流，避免重复安装自定义脚本或分阶段不一致的命令。
- 2026-07-20：工作流已加入格式、分析、测试与 Android Debug 构建命令。本地等价全量测试暴露既有高保真导航后遗留的 `app_shell_test` 断言及若干安全存储测试环境问题；已修复队列恢复在无 SQLite 环境的未处理异常、音源 URL 偏好和 WebDAV 凭据在安全存储不可用时的安全降级。
- 2026-07-20：复查确认 `app_shell_test` 的 SQLite 异常来自测试直接启动完整 `ProviderScope` 却未配置 sqflite factory，不应在生产 `LibraryStore` 中加入测试环境分支；队列恢复的额外错误捕获不能修复该测试架构问题，已撤回。后续在 CI 任务中为 Widget Test 注入专用内存 SQLite factory/存储替身，并同步更新过期导航断言。

## 验收与下一步

- 待执行：更新旧 Widget Test 的导航/布局断言并完成全量通过；远端首次运行结果待仓库推送后确认。
- 关联：`P1-01`、`P7-05`。
