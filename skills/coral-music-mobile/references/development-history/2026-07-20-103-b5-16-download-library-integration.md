# B5-16 已下载歌曲进入本地媒体库

- 阶段：Batch 5 / Phase 4
- 状态：DOING
- 负责人：Codex
- 开始时间：2026-07-20
- 完成时间：未完成

## 目标、范围与依赖

让完成下载的歌曲同时出现在既有音乐分类中，并作为本地文件直接播放；复用下载任务的 Track 快照和目标路径。依赖 B5-14 分类、B6-01 下载任务。

不复制下载文件、不把临时 URL/音源脚本写入媒体库，也不把未完成或文件缺失任务当作可播放本地音乐。

## 实际修改、验证与后续

- `LibraryStore.listLibraryTracks` 在既有列表/收藏曲目后合并已完成且文件仍存在的下载任务，转为 `download` 本地 URI Track；音乐分类、队列和播放复用原有路径。
- 未完成、已取消、失败或被系统清理的下载不会进入媒体库；凭据、临时 URL 和 User API 内容不写入任何新的持久化位置。
- `dart format`、`flutter analyze` 和 `git diff --check` 通过。
- 状态：DONE。流派/年份需要可靠跨格式标签读取后再加入，不能从文件名推断。
