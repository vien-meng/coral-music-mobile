# B6-07 定时停止

- 阶段：Batch 7 / Phase 6
- 状态：DOING
- 负责人：Codex
- 开始时间：2026-07-20
- 完成时间：未完成

## 范围

在播放器实现指定分钟后停止与当前歌曲结束后停止。计时只在本次应用会话生效，使用现有 `AudioEngine.stop()`；不伪造系统后台闹钟或跨重启定时。

## 实际修改

- `PlayerController` 维护会话内截止时间和“当前曲结束后停止”状态；两种模式互斥，控制器 dispose 时取消计时器。
- 播放详情新增真实定时操作，提供 15/30/45/60 分钟、当前曲结束和关闭入口。
- 曲目完成时若已设置当前曲结束停止，停止引擎且不选择下一曲；新增播放器回归覆盖。`flutter analyze`、`flutter test test/player_controller_test.dart`、`git diff --check` 通过。
- 状态：DONE。跨重启定时与系统后台闹钟不在移动端首发范围。
