# B6-06 下载文件命名

- 阶段：Batch 6 / Phase 5
- 状态：DOING
- 负责人：Codex
- 开始时间：2026-07-20
- 完成时间：未完成

## 目标与范围

让默认下载文件名使用 `歌名 - 歌手.扩展名`；同名文件或临时文件存在时自动加序号，保证不会覆盖已有下载。

不做：用户自定义模板、系统下载目录导出、标签写入或文件管理器集成。

## 依赖与验证

- 复用 B6-01 的应用私有目录、`.part` 原子写入与恢复逻辑。
- 完成后用最小路径选择回归与静态检查验证。

## 实际修改

- 下载路径现在优先为 `歌名 - 歌手.扩展名`；歌手为空时退化为 `歌名.扩展名`。
- 最终文件和 `.part` 临时文件都参与冲突判断；发生重名时使用 `(2)`、`(3)` 顺序后缀，不覆盖任何已有下载。
- `flutter analyze`、`flutter test test/download_controller_test.dart` 和 `git diff --check` 通过。
- 状态：DONE。用户自定义模板和系统导出继续由 P5-02 后续范围承接。
