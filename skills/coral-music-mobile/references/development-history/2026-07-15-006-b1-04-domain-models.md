# B1-04 建立领域模型

- 阶段：Batch 1
- 状态：DONE
- 负责人：Codex
- 开始时间：2026-07-15
- 完成时间：2026-07-15

## 目标与范围

定义 `OnlineSource`、`Track`、`AudioQuality`、`LeaderboardBoard`、`PageResult<T>` 和 `AppFailure`。依赖 B1-01；不为未开发模块创建模型。

## 桌面端基线与数据变化

对照桌面 `onlineMusicService.ts` 和 `leaderboardStore.ts`。`Track.id` 使用来源类型、来源和来源歌曲 ID 组合，不使用歌名/歌手作为主键。

## 实际修改与验证

- 新建 `lib/domain/music.dart`，实现五源枚举、来源类型、音质、`Track`、榜单和分页结果。
- `Track.id` 固定为 `sourceKind:sourceId:sourceTrackId`。
- 单元测试覆盖稳定 ID 和分页边界；测试通过。

## 风险、阻塞与后续

只创建当前排行榜和队列已有调用方的类型；列表、下载和WebDAV模型后置。关联 B1-05、B2-02、B2-05。
